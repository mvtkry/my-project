name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Zip files
        run: zip -r project.zip .

      - uses: actions/upload-artifact@v3
        with:
          name: project
          path: project.zip
          
